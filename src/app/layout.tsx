import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Ginas Giant Gummies - Professional Gym Services',
  description: 'Professional Gym services with Class Booking, Trainer Profiles, Membership Plans, Progress Tracking. Contact us today for exceptional service.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}